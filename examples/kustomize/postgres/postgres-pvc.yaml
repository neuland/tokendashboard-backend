apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tokendashboard-postgres-pvc
  namespace: my-namespace
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard
