apiVersion: v1
kind: Service
metadata:
  name: tokendashboard-backend-service
  namespace: my-namespace
spec:
  selector:
    app: tokendashboard-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
